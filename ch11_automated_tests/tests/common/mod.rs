/*
You can also group the integration test functions.
Each file in the `tests` directory is compiled as its own separate crate
The different behavior of files is most noticeable,
    when you have a set of helper functions to use in multiple integration tests.
*/

pub fn setup() {
    // ...
}